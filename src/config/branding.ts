/** User-facing application branding. Keep CareFlow as the system/repository identity. */
export const APP_BRAND = {
  displayName: "Claims Informatics",
  shortName: "Claims Metric",
  description: "Insurance Authorization & Claims Management",
} as const;
